# Staging LOCAL (docker). Imita al VPS; NO son las credenciales de producción.
# Uso:
#   docker compose -f docker-compose.staging.yml --env-file .env.staging up -d --build

# ── Base de datos (dueño de las tablas: migra, siembra, administra RLS) ──
POSTGRES_USER=ventafacil
POSTGRES_PASSWORD=staging_owner_pw
POSTGRES_DB=ventafacil

# ── Rol de aplicación (SIN privilegios: con este corre el API) ──
# Es la pieza clave de RLS: Postgres ignora las políticas para superusuarios y para el
# dueño de las tablas, así que el API NO puede conectarse con ellos.
APP_DB_ROLE=ventafacil_app
APP_DB_PASSWORD=staging_app_pw

# 1 = crear el rol y activar RLS al arrancar el contenedor.
ENABLE_RLS=1

# ── API ──
JWT_ACCESS_SECRET=staging_access_secret_no_usar_en_produccion
JWT_PLATFORM_SECRET=_QZ5nmnnDFSyd2LxXDslPchTquuAH-2djTtiUbk6AYkpJJiR
JWT_REFRESH_SECRET=staging_refresh_secret_no_usar_en_produccion
APP_URL_TEMPLATE=http://{slug}.localhost:5174

# Topes SUBIDOS sólo para staging: aquí se ejercitan los flujos completos desde una
# sola IP y los valores reales (20 logins/5min, 5 altas/hora) cortarían las pruebas.
# Los valores de producción están en .env.example.
# El tope global es por IP y toda la carga de prueba sale de una sola: sin subirlo,
# la prueba mediría el limitador en vez del servidor.
RATE_LIMIT_MAX=1000000
LOGIN_RATE_LIMIT_MAX=2000
REGISTER_RATE_LIMIT_MAX=500
EXPORT_RATE_LIMIT_MAX=500
DB_POOL_MAX=8

# Obligatoria con NODE_ENV=production: el API no arranca sin ella.
# 5174 = la web apuntando a staging (5173 queda para la web de desarrollo).
CORS_ORIGINS=http://*.localhost:5174,http://*.localhost:5175,http://localhost:5174,http://localhost:5175,http://localhost:5173
