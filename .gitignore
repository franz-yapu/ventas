node_modules
dist
build
.turbo
coverage
*.log
.env
.env.local
.DS_Store
playwright-report
test-results
pnpm-debug.log*
.vite
dev-dist

# Backups locales de staging (no versionar volcados de datos)
backups/

# Config local de Claude Code (permisos de esta maquina).
# Se ignora por nombre, no la carpeta: .claude/settings.json y las skills del
# proyecto SI deberian versionarse si algun dia se añaden.
.claude/settings.local.json

# Fotos de producto subidas en desarrollo (MEDIA_DIR). En el VPS esto es un volumen.
media/
